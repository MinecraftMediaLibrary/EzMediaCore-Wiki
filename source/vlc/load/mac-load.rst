Loading VLC Binaries on Mac
==============================================================

Mac uses .DYLIB extensions. When we find the plugin path folder,
it is automatically set to "/lib/../plugins" (relative to the
plugin folder). Heuristics are enabled by default.
